---
version: "0.1"
globalTimeout: 15
testSuiteTimeout: 10
testSuiteStep: 5
runson: linux
autosplit: true
concurrency: 2
retryOnFailure: false
maxRetries: 0

runtime:
  language: node
  version: "20"

pre:
  - npm ci

testDiscovery:
  type: raw
  mode: dynamic
  command: ls tests/*.spec.js

testRunnerCommand: npx playwright test $test

env:
  LT_USERNAME: anmolsoin
  LT_ACCESS_KEY: ${LT_ACCESS_KEY}

jobLabel: ['he-playground', 'fw-grid-cdp']
